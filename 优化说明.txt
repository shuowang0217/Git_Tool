项目版本助手_优化版_v1.0.2

本次修复：
1. 修复“一键保存并上传”不会弹出版本说明输入框的问题。
2. 普通“保存版本”和“一键保存并上传”统一使用同一个版本说明输入逻辑。
3. 一键保存并上传时默认说明仍为 save: 时间，但允许用户手动修改。
4. 用户在版本说明窗口点击取消或输入为空时，会停止本次保存上传，不会自动提交默认说明。

保留 v1.0.1 优化：
1. 初始化仓库默认分支由 main 改为 master。
2. current_branch() 兜底返回由 main 改为 master。
3. 初始化完成提示中增加当前主分支名称。
4. 压缩包去掉旧 dist/exe，避免误用旧版本 exe。
